<UserControl x:Class="NFL_Data_Spy.Components.SingleMatchupView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
             xmlns:local="clr-namespace:NFL_Data_Spy.Components"
             mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="200">

    <materialDesign:Card
  Width="200">
        <materialDesign:Card.Effect>
            <DropShadowEffect BlurRadius="10"
                              ShadowDepth="6"
                              Direction="270"
                              Color="Black"
                              Opacity=".6" />
        </materialDesign:Card.Effect>
    <Grid>
    <Grid.RowDefinitions>
      <RowDefinition
        Height="140" />
      <RowDefinition
        Height="*" />
      <RowDefinition
        Height="Auto" />
    </Grid.RowDefinitions>
    <Image
      Width="196"
      Height="140"
      Source="/Images/Logo/ARI.png"
      Stretch="UniformToFill" />
    <Button
      Grid.Row="0"
      Margin="0,0,16,-20"
      HorizontalAlignment="Right"
      VerticalAlignment="Bottom"
      Style="{StaticResource MaterialDesignFloatingActionMiniSecondaryButton}">
      <materialDesign:PackIcon
        Kind="FootballAmerican"
              Foreground="{StaticResource Primary300ColorBrush}"/>
    </Button>
    <StackPanel
      Grid.Row="1"
      Margin="8,24,8,0">
      <TextBlock
        FontWeight="Bold" 
        Text="{Binding Name}"/>

      <TextBlock
        VerticalAlignment="Center"
        Text="A great way to keep fit and forget about the constant grind of IT."
        TextWrapping="Wrap" />
    </StackPanel>
    <StackPanel
      Grid.Row="2"
      Margin="8"
      HorizontalAlignment="Right"
      Orientation="Horizontal">
      <Button
        Width="30"
        Padding="2,0,2,0"
        materialDesign:RippleAssist.IsCentered="True"
        Style="{StaticResource MaterialDesignToolButton}">
        <materialDesign:PackIcon
          Kind="ShareVariant" />
      </Button>
      <Button
        Width="30"
        Padding="2,0,2,0"
        materialDesign:RippleAssist.IsCentered="True"
        Style="{StaticResource MaterialDesignToolButton}">
       <materialDesign:PackIcon
          Kind="Heart" />
          </Button>
     </StackPanel>
  </Grid>
</materialDesign:Card>  

</UserControl>
